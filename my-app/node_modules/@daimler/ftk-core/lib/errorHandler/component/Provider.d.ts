import * as React from 'react';
export interface IState {
    error: string;
}
declare class Provider extends React.Component<{}, IState> {
    state: IState;
    private errorHandler;
    private bindServices;
    render(): JSX.Element;
    componentDidCatch(error: Error): void;
}
export default Provider;
