import { EventEmitter } from './EventEmitter';
import IEventData from './interface/IEventData';
export declare class InterconnectionService {
    private name;
    private isHotLoaded;
    private eventEmitter;
    constructor(name: string, isHotLoaded: boolean, eventEmitter: EventEmitter);
    subscribe<TData>(event: string, callback: (data: IEventData<TData>) => void): () => void;
    publish<TData>(event: string, data: TData): void;
}
