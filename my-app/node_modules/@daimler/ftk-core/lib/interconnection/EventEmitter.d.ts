export declare class EventEmitter {
    private eventEmitter;
    subscribe<TData>(event: string, callback: (data: TData) => void): () => void;
    publish<TData>(event: string, data: TData): void;
}
