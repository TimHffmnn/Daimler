/// <reference types="react" />
import { IntlShape } from 'react-intl';
import { Registry } from '../../util/Registry';
import ITranslation from './interface/ITranslation';
export declare class I18nService {
    private registry;
    private langGetter;
    private langSetter;
    private intl;
    constructor(registry: Registry<ITranslation>, langGetter: () => string, langSetter: (lang: string) => void, intl: IntlShape);
    translate(translationId: string, params?: object): JSX.Element | string;
    translateToString(translationId: string, params?: any): string;
    setLang(lang: string): void;
    getLang(): string;
    getLangs(): string[];
}
