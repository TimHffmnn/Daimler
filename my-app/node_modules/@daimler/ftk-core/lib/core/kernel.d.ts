import * as React from 'react';
import { IDiContainer } from '..';
/**
 * @deprecated Use <App> instead
 */
export default function kernel<TProps = {}>(name: string, configurationContainer: IDiContainer): React.ComponentType<TProps>;
