import { IDiContainer } from '..';
declare function registerDefaultDependencies(container: IDiContainer, name: string): void;
declare namespace registerDefaultDependencies {
    var displayName: string;
}
export default registerDefaultDependencies;
