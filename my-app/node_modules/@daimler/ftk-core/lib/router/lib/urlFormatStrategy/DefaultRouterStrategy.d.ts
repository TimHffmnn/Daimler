import IUrlFormatStrategy from './interface/IUrlFormatStrategy';
declare class DefaultRouterStrategy implements IUrlFormatStrategy {
    private name;
    private baseUrl;
    constructor(name: string, baseUrl: string);
    extract(url: string): string;
    format(url: string): string;
}
export default DefaultRouterStrategy;
