import IUrlUtil from './interface/IUrlUtil';
import IUrlStrategy from './strategy/interface/IUrlStrategy';
/**
 * @class Url
 * @implements IUrl
 */
declare class Url implements IUrlUtil {
    /**
     * @type {IUrlStrategy}
     */
    strategy: IUrlStrategy;
    /**
     * @constructor
     * @param {IUrlStrategy} strategy
     */
    constructor(strategy: IUrlStrategy);
    /**
     * @param url
     * @returns {string[]}
     */
    extractPath(url: string): string[];
    /**
     * @param segments
     * @returns {string}
     */
    buildPathFromSegments(segments: string[]): string;
    /**
     * @param url
     * @returns {string}
     */
    extractHashbang(url: string): string;
    /**
     * @param hash
     * @returns {string}
     */
    buildHashbang(hash: string): string;
    /**
     * @param url
     * @returns {any}
     */
    extractQuery(url: string): any;
    /**
     * @returns {string}
     */
    buildQueryFromParameters(params?: any): string;
}
export default Url;
